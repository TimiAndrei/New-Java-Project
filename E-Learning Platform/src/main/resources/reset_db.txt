psql -h localhost -p 5433 -U timi -d elearning_db

DO $$ DECLARE
    r RECORD;
BEGIN
    FOR r IN (SELECT tablename FROM pg_tables WHERE schemaname = 'elearning_schema') LOOP
        EXECUTE 'DROP TABLE IF EXISTS elearning_schema.' || quote_ident(r.tablename) || ' CASCADE';
    END LOOP;
END $$;